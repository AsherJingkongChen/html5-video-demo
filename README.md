# HTML5 `<Video>` Demo

## Run

1. Install a bulk of packages

    ```shell
    pip install -Ur requirements.txt
    ```

2. Run the server slowly

    ```shell
    python3 main.py
    ```
    Or
    ```shell
    ./main.py
    ```
